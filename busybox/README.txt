Busybox for Panwah:
===================
This copy of BusyBox is linked to libbusybox (/usr/lib/libbusybox.so.@PKGVER@),
but also include a binary without linked to libbusybox (/usr/share/doc/busybox/busybox)

# APPLETS LIST
